diff --git a/node_modules/@inkjs/ui/build/components/text-input/use-text-input-state.js b/node_modules/@inkjs/ui/build/components/text-input/use-text-input-state.js
index 1e4803d..1d9b655 100644
--- a/node_modules/@inkjs/ui/build/components/text-input/use-text-input-state.js
+++ b/node_modules/@inkjs/ui/build/components/text-input/use-text-input-state.js
@@ -33,6 +33,14 @@ const reducer = (state, action) => {
                 cursorOffset: newCursorOffset,
             };
         }
+        case 'replace': {
+            return {
+                ...state,
+                previousValue: state.value,
+                value: action.text,
+                cursorOffset: action.text.length,
+            };
+        }
     }
 };
 export const useTextInputState = ({ defaultValue = '', suggestions, onChange, onSubmit, }) => {
@@ -41,14 +49,16 @@ export const useTextInputState = ({ defaultValue = '', suggestions, onChange, on
         value: defaultValue,
         cursorOffset: defaultValue.length,
     });
-    const suggestion = useMemo(() => {
+    const fullSuggestion = useMemo(() => {
         if (state.value.length === 0) {
             return;
         }
         return suggestions
-            ?.find(suggestion => suggestion.startsWith(state.value))
-            ?.replace(state.value, '');
+            ?.find(suggestion => suggestion.toLowerCase().startsWith(state.value.toLowerCase()));
     }, [state.value, suggestions]);
+    const suggestion = useMemo(() => {
+        return fullSuggestion?.slice(state.value.length);
+    }, [fullSuggestion, state.value]);
     const moveCursorLeft = useCallback(() => {
         dispatch({
             type: 'move-cursor-left',
@@ -70,14 +80,21 @@ export const useTextInputState = ({ defaultValue = '', suggestions, onChange, on
             type: 'delete',
         });
     }, []);
+    const replace = useCallback((text) => {
+        dispatch({
+            type: 'replace',
+            text,
+        });
+    }, []);
     const submit = useCallback(() => {
         if (suggestion) {
             insert(suggestion);
-            onSubmit?.(state.value + suggestion);
+            const fullSuggestion = suggestions?.find(s => s.toLowerCase().startsWith(state.value.toLowerCase()));
+            onSubmit?.(fullSuggestion ?? (state.value + suggestion));
             return;
         }
         onSubmit?.(state.value);
-    }, [state.value, suggestion, insert, onSubmit]);
+    }, [state.value, suggestion, suggestions, insert, onSubmit]);
     useEffect(() => {
         if (state.value !== state.previousValue) {
             onChange?.(state.value);
@@ -86,9 +103,11 @@ export const useTextInputState = ({ defaultValue = '', suggestions, onChange, on
     return {
         ...state,
         suggestion,
+        fullSuggestion,
         moveCursorLeft,
         moveCursorRight,
         insert,
+        replace,
         delete: deleteCharacter,
         submit,
     };
diff --git a/node_modules/@inkjs/ui/build/components/text-input/use-text-input.js b/node_modules/@inkjs/ui/build/components/text-input/use-text-input.js
index 6247176..5920360 100644
--- a/node_modules/@inkjs/ui/build/components/text-input/use-text-input.js
+++ b/node_modules/@inkjs/ui/build/components/text-input/use-text-input.js
@@ -41,10 +41,15 @@ export const useTextInput = ({ isDisabled = false, state, placeholder = '', }) =
         if (key.upArrow ||
             key.downArrow ||
             (key.ctrl && input === 'c') ||
-            key.tab ||
             (key.shift && key.tab)) {
             return;
         }
+        if (key.tab) {
+            if (state.suggestion && state.fullSuggestion) {
+                state.replace(state.fullSuggestion);
+            }
+            return;
+        }
         if (key.return) {
             state.submit();
             return;
